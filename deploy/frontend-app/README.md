# Frontend App

Compose para instancias privadas de frontend.

## Uso

1. Copie `.env.example` para `.env`.
2. Ajuste a imagem do frontend.
3. Suba o servico:

```bash
docker compose --env-file .env up -d
```
